var services = function(){
	var http = null;
	/******************************************************/
	/******************   Sách được thuê   ****************/
	/*	Input
	args = {
		quantity: number		// Số lượng sách hiển thị (default: 10)
		offset: number			// Số lượng sách bỏ qua (default: 0)
		orderby: string			// Trường cần sort (defaul: id)
		asc: bool				// Chiều sắp xếp tăng (defaul: true)
		filters: array			// Các trường cần trả về (default: [])
	}
	*/
	/*	Output
		Thành công
			success(response)
	*/
	/******************************************************/
	function getBookRented(args, success=false, error=false){
		// $http.get("api.htm")
		// .then(function(response) {
		// 	if (success) success(response);
		// }, function(response) {
		// 	if (error) error(response);
		// });
	}
}